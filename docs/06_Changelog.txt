═══════════════════════════════════════════════════════════════════════════
  ЖУРНАЛ ЗМІН (CHANGELOG)
═══════════════════════════════════════════════════════════════════════════

Серпень 2026 (останнє оновлення)
──────────────────────────
  ✔ МУЛЬТИ-LLM У РОЗГАЛУЖЕНИХ СЦЕНАРІЯХ
    • Вкладка «Розгалужені сценарії» тепер підтримує всіх провайдерів
      (Gemini, Groq, Mistral, OpenRouter, Cerebras, OpenAI, Anthropic,
      власний сервер), як і генератор тестів
    • Після введення ключа кнопка «Отримати моделі з сервера»
      завантажує список доступних моделей для вибору
    • Інструкції з отримання ключа для кожного провайдера
  ✔ ВИПРАВЛЕНО ГЕНЕРАЦІЮ ПИТАНЬ-ВІДПОВІДНОСТІ (matching)
    • LLM більше не повертає звичайні тести замість matching:
      додано явний блок-інструкцію з обов'язковим форматом
      «ліва частина -> права частина» та прикладом
  ✔ РЕАЛЬНА ДИФЕРЕНЦІАЦІЯ РІВНІВ СКЛАДНОСТІ
    • Легка — знання/розуміння (низ Bloom), очевидна відповідь,
      без пасток
    • Середня — застосування/аналіз, правдоподібні дистрактори,
      інформаційний шум, питання на пріоритет
    • Складна — аналіз/оцінка/синтез (верх Bloom) з когнітивними
      пастками, застарілими/хибними практиками як дистракторами,
      питаннями на пріоритизацію та кількома кроками міркування
  ✔ ПІДТРИМКА ФАЙЛІВ .doc (старий бінарний формат Word 97-2003)
    • Стратегія читання (що доступне на комп'ютері користувача):
      1) LibreOffice headless → txt (найкраща якість)
      2) antiword  3) MS Word COM-автоматизація (якщо Word встановлено)
      4) резервний варіант: витяг тексту з бінарного потоку
      (UTF-16LE / cp1251) — зіркові маркери та літерний формат
      розпізнаються і в цьому разі
  ✔ ГЕНЕРАТОР ТЕСТІВ ПЕРЕРОБЛЕНО: МУЛЬТИ-LLM
    • 8 провайдерів: Google Gemini, Groq, Mistral (La Plateforme),
      OpenRouter, Cerebras — безкоштовні рівні; OpenAI та
      Anthropic (Claude) — платні; OpenAI-сумісний власний сервер
      (Ollama / LM Studio / vLLM) — локально, без ключа
    • Кнопка «❓ Як отримати ключ» — покрокова інструкція для кожного
      провайдера (посилання на консоль, реєстрація, ліміти)
    • Після введення API-ключа кнопка «🔄 Отримати моделі з сервера»
      запитує у провайдера список ДОСТУПНИХ моделей, і користувач
      обирає лише з наявних (для OpenRouter безкоштовні «:free»
      показуються першими)
  ✔ НОВІ МОДУЛІ ПРОГРАМИ (інтерфейс поділено на 5 вкладок)
    • «👥 Списки студентів» — створення списків для груп Moodle:
      розпізнавання «Прізвище Ім'я», «Прізвище Ім'я По-батькові»,
      «Прізвище, Ім'я, email»; автоматична транслітерація username
      (укр → lat); валідація рядків з попередженнями; експорт у
      CSV «Upload users» (username,password,firstname,lastname,email,
      course1,group1), CSV для когорт (cohort1) та XLSX
    • «✨ Генерація тестів (Gemini)» — генерація тестів через Google
      Gemini: API-ключ вводиться на початку; настройки: тема, кількість
      питань (1-100), тип (вибір однієї/кількох, так/ні, коротка,
      відповідність, змішаний), складність, аудиторія, кількість
      варіантів (2-6), фідбек, додаткові вимоги; результат одразу
      експортується в GIFT/Aiken/XML або зберігається як TXT
    • «🌿 Розгалужені сценарії» — створення branching scenarios з
      повним набором настройок (topic, audience, difficulty easy/medium/
      hard, learning objectives, context, style, scoring model,
      constraints). Системний промпт вбудований і додається
      автоматично (структура SCENARIO_TITLE / LEARNING_OBJECTIVES /
      NODE_x / END_x / INSTRUCTOR_GUIDE / MOODLE_GIFT_EXPORT, усе
      українською, SCORE_CHANGE, типи результатів SUCCESS/
      PARTIAL_SUCCESS/FAILURE/CRITICAL_FAILURE). Кнопка «Показати
      дерево» читає згенерований сценарій БЕЗ розмітки і відображає
      ієрархічну структуру (вузли → варіанти → переходи → кінцівки).
      Окремий експорт секції MOODLE_GIFT_EXPORT у файл для імпорту
    • «🔗 Moodle API» — з'єднання з Moodle Web Services (REST):
      перевірка токена (core_webservice_get_site_info), список курсів,
      завантаження файлів у draft-область (core_files_upload).
      Прямий імпорт у банк питань потребує плагіна qformat на боці
      Moodle — модуль готовий до виклику довільних WS-функцій
  ✔ ІНСТАЛЯТОР ОДНИМ ФАЙЛОМ (MoodleTestConverter_Setup.exe)
    • NSIS-інсталятор зі сторінкою ліцензійної угоди (прийняття
      обов'язкове перед встановленням)
    • Встановлення в Program Files, ярлики в меню «Пуск» і на робочому
      столі, повний деінсталятор (Uninstall.exe), запис у реєстрі
    • Тиха установка: MoodleTestConverter_Setup.exe /S
  ✔ КНОПКА «ℹ️ Інформація» у програмі — повна ліцензійна угода:
    відмова від відповідальності розробника, відповідальність
    користувача, відсутність гарантій, конфіденційність (робота
    повністю офлайн), сторонні компоненти
  ✔ НОВІ РОЗМІТКИ ТА ВИПРАВЛЕННЯ СПИСКІВ
    • Зірочка ПЕРЕД літерою без пробілу: «*В. відповідь»
      (зірочка — окремий run у Word, приклеєна до літери)
    • Зірочка ПІСЛЯ тексту варіанта з літерами: «Б. 324 *»
    • Ненумеровані питання (без «1.»): рядок із «?» або довге
      речення після завершеного блоку варіантів починає нове питання
    • Багаторядкові параграфи Word (питання + всі варіанти в одному
      параграфі, розділені переносом рядка) розбиваються коректно
    • Виправлено злиття питань при ненумерованих і змішаних списках
  ✔ ВИБІР РЕЖИМУ ПРАВИЛЬНИХ ВІДПОВІДЕЙ
    • GUI: радіокнопки «Декілька (автовизначення)» / «Одна (лише
      перша позначена)» — працює і для перегляду, і для пакетної
      конвертації
    • CLI: прапорець --single
    • Режим «Декілька»: бали розподіляються рівномірно між усіма
      позначеними відповідями (GIFT: ~%33.33333%..., XML: fraction,
      <single>false</single>)
    • Режим «Одна»: лише перша позначена відповідь лишається
      правильною (100%), <single>true</single> в Moodle XML
  ✔ Формат дампів медичних баз: newTem; + name: (назва теми стає
    категорією Moodle), QuestName: + trueNum:N
  🐛 Виправлено розподіл балів у multi-режимі (було 100% кожній
    правильній, стало рівномірний розподіл)
  ⚡ МАСОВА КОНВЕРТАЦІЯ
    • Кнопка «📂 Додати папку…» — рекурсивно додає всі підтримувані
      файли (.txt .docx .xlsx .csv .html) з папки та підпапок
    • Пакетна обробка у фоновому потоці — GUI не зависає
    • Прогрес-бар + статус-рядок («Обробка 5/22: файл.txt»)
    • Попередження про файли, які Aiken пропустив (не-single-answer)

  🛡 ЗАХИСТ ВІД ДУРАКА
    • Файл без жодної позначеної правильної відповіді → відхиляється
      з поясненням (замість мовчазного позначення першого варіанта)
    • Файл без варіантів відповідей (голий список питань) → відхиляється
    • «Питання», чия єдина «відповідь» — це наступне пронумероване
      питання → відхиляється (захист від злиття)
    • Перевірка існування файлів і форматів перед конвертацією
    • Захист від завеликих файлів (MemoryError)
    • Понятне повідомлення, коли папка не містить підтримуваних файлів

  ✔ НОВІ КЕЙСИ (10 додаткових, всього 23)
    • case14: формули з двокрапками («Закон Ома: I = U/R»)
    • case15: багаторядкові питання (текст питання на 2-3 рядках)
    • case16: відповіді з дужками та лапками
    • case17: файл без відповідей → відхилення (захист)
    • case18: всі відповіді позначені правильними → multi-select
    • case19: змішана нумерація (1. + 2) + №3.)
    • case20: коментарі «//» між питаннями
    • case21: QuestName без порожніх рядків між питаннями
    • case22: дуже довгі відповіді (>200 символів)
    • case23: спецсимволи GIFT (=, ~, #, {, }) в тексті

  🐛 ВИПРАВЛЕННЯ
    • Багаторядкові питання більше не зливаються з наступним питанням
    • Питання-речення з кількома словами («2. При серцево-легеневій…»)
      не помиляється за числовий варіант відповіді
    • Питання, що закінчуються на «?»/«:», завжди починають нове питання

Попереднє оновлення
──────────────────────────
  ✔ НОВІ ФОРМАТИ МАРКУВАННЯ
    • Формат «QuestName:» (експорт з медичних/екзаменаційних баз питань):
      питання без нумерації, варіанти — прості рядки без літер,
      правильна відповідь — за номером варіанта
    • Ключ «trueNum:3» — номер правильного варіанта (1-based)
    • Ключі «Ansver:3» / «Anser:2» (поширені друкарські помилки)
    • Ключ «Правильна відповідь:3» — без пробілу після двокрапки

  🐛 ВИПРАВЛЕННЯ
    • Числові відповіді з двокрапкою («15:2», «30:2») більше не
      позначаються помилково як правильні через регекс «correct»
    • Рядки-ключі в QuestName-режимі не додаються як варіанти відповіді

Попереднє оновлення
──────────────────────────
  ⚡ ОПТИМІЗАЦІЯ ЗАПУСКУ
    • Збірка змінена з --onefile на --onedir:
      exe 16 МБ → 4 МБ, запуск ~6 с → ~1 с
    • Ліниві імпорти: docx та openpyxl завантажуються лише при потребі
    • Ліниве завантаження ядра в GUI: вікно з'являється миттєво

  ✔ НОВІ ФОРМАТИ РОЗМІТКИ (університетські)
    • Пари «+ / -» перед варіантом відповіді
    • Ключі «Ключ: 1-а; 2-б» у кінці файлу
    • Числові ключі «Відповідь: 2» (за номером варіанта)
    • «Питання №2.», «Запитання 1.», «Завдання 3.» — ключові слова
    • Markdown: **жирний** для позначення відповіді
    • Червоний колір шрифту у DOCX
    • Табличні шаблони Word (№ | Питання | Відповіді | Правильна)
    • Цифрові варіанти відповідей: «1) 90°», «2) 180° *»

  🐛 ВИПРАВЛЕННЯ
    • «Невірно» більше не обрізається до «Не» (word boundary у регексі)
    • Рядки-ключі не зливаються з останньою відповіддю
    • Заголовок файлу не стає питанням
    • Короткі числові відповіді («3. 360°») не розпізнаються як питання

Початковий реліз
──────────────────────────────
    • Парсинг .txt, .docx, .xlsx, .csv, .html
    • Позначення: зірочка *, жирний шрифт, кольорове виділення
    • Ключі: «Відповідь: Б», «ANSWER: B», «Відповіді: 1-Б, 2-В»
    • Типи питань: multichoice, truefalse, shortanswer, matching,
      numerical, essay
    • Експорт: GIFT, Moodle XML, Aiken
    • GUI (tkinter, українська) + CLI (argparse)
    • Кодування: UTF-8, UTF-8-BOM, Windows-1251

────────────────────────────────────────────────────────
Серпень 2026 — Генератор повних курсів Moodle (.mbz)
────────────────────────────────────────────────────────
• Новий модуль mbz_module.py: генерація повної резервної копії курсу
  Moodle у форматі .mbz (Moodle Backup 2.x) за специфікацією
  MOODLE_MBZ_SPECIFICATION.md:
  - повне дерево XML: moodle_backup.xml, questions.xml, gradebook.xml,
    course/, sections/, activities/quiz_N/ (module, quiz, inforef,
    grades, roles);
  - узгоджений ID-mapping (Course=2, Context=10, Section 1..N,
    Module 1..N, Question 1001..N, Answer 10001..N);
  - пресети Review Options: standard (69888/4352), strict, full (69904);
  - глобальні налаштування тесту (час, спроби, метод оцінювання,
    прохідний бал, перемішування) задаються ОДИН раз на весь курс
    і застосовуються до кожного quiz.xml.
• Вкладка «Генератор курсу» розширена:
  - масове додавання файлів курсу перетягуванням (drag-and-drop,
    tkinterdnd2, з безпечним fallback на кнопки);
  - підтримка сирих файлів усіх форматів (.txt .doc .docx .xlsx
    .csv .html) — не-txt автоматично конвертуються через ядро;
  - вибір формату виходу: повний курс .mbz АБО банк питань Moodle XML;
  - кнопка «Експорт сирих файлів»: пакетна конвертація docx/doc/xlsx/
    csv/html у нормалізований Aiken-.txt у вибрану папку;
  - список файлів курсу у вигляді Listbox із прокруткою.
• Документація: додано docs/MOODLE_MBZ_SPECIFICATION.md (повна
  технічна специфікація формату .mbz українською).
• Тести: test_mbz.py + test_course_integration.py + verify_final.py —
  30 файлів, 112 питань, 0 помилок; .mbz валідовано (усі XML
  парсяться, ASCII-шляхи, review-flags, sequence, ID-mapping).

────────────────────────────────────────────────────────
Серпень 2026 — Плоскі (unlettered) списки відповідей у DOCX
────────────────────────────────────────────────────────
• converter_core.py: додано розпізнавання ПЛОСЬКИХ списків відповідей,
  де номери/маркери живуть у форматуванні Word (автонумерація numId),
  а не в тексті. Підтримано 7 варіантів розмітки одного питання:
  автосписок/звичайний текст × правильна відповідь (жирний/зірочка *).
• Логіка: рядок із '?' = питання; наступні короткі рядки без '?' =
  окремі варіанти; правильна за зірочкою, жирним, підкресленням,
  кольором або виділенням. Заголовок документа (короткий рядок без
  '?' перед першим питанням) автоматично відсікається.
• Новий тестовий кейс: tests_uni2/case28_plain_options.docx (7 питань).
• Регресія: 31 файл, 119 питань, 0 проблем (усі попередні формати
  збережено без змін).

────────────────────────────────────────────────────────
Серпень 2026 — Виправлення формату .mbz під реальний бекап Moodle 4.5
────────────────────────────────────────────────────────
• КРИТИЧНО: проаналізовано реальний .mbz (Moodle 4.5.2, IFNMU) —
  формат контейнера виявився TAR.GZ зі службовим індексом
  .ARCHIVE_INDEX, а НЕ zip. mbz_module.py повністю переписано:
  - контейнер tar.gz (сигнатура 1F 8B), .ARCHIVE_INDEX першим
    записом із коректним Count і tab-розділеними рядками;
  - questions.xml: питання обгорнуті в Moodle 4.x-схему
    question_bank_entries → question_bank_entry → question_version →
    question_versions → questions (status=ready);
  - quiz.xml: слоти оформлені як <question_instance> із
    <question_set_reference> (filtercondition JSON), додано обов'язкове
    поле <reviewmaxmarks>, порожні <overrides>/<grades>/<attempts>,
    <subplugin_quizaccess_seb_quiz>, <quiz_grade_items>;
  - gradebook.xml: додано <attributes>, decimals=$@NULL@$, hidden у
    grade_category;
  - module.xml/course.xml: поля Moodle 4.5 (downloadcontent, tags,
    courseformatoptions, category, pdfexportfont тощо);
  - додано службові файли: badges.xml, completion.xml,
    grade_history.xml, users.xml, moodle_backup.log, course/{filters,
    comments,calendar,contentbank,completiondefaults,competencies}.xml,
    activities/quiz_N/{filters,grade_history,completion,comments,
    calendar,xapistate,competencies}.xml;
  - review-пресети оновлено (standard/strict/full) з reviewmaxmarks.
• Тести test_mbz.py / test_course_integration.py оновлено під tar.gz:
  перевірка сигнатури gzip, .ARCHIVE_INDEX, question_bank_entries,
  question_instance, review-flags, ASCII-шляхів, валідності всіх XML.
• Порівняльна перевірка: згенерований .mbz і реальний бекап читаються
  однаковим кодом (tar.gz + .ARCHIVE_INDEX + moodle_backup.xml) ✔
• docs/MOODLE_MBZ_SPECIFICATION.md оновлено під реальний формат
  (контейнер, ієрархія, XML-схеми 4.5, чекліст валідації).
• Новий кейс: tests_uni2/case29_real_backup.mbz (реальний бекап IFNMU).
• Регресія: 32 файли, 119 питань, 0 проблем.

────────────────────────────────────────────────────────
Серпень 2026 — Виправлення порожніх тестів після Restore (.mbz)
────────────────────────────────────────────────────────
• ВИПРАВЛЕНО КРИТИЧНУ ПОМИЛКУ: тести після відновлення курсу в Moodle
  були порожні (без питань). Причина — слоти тесту генерувалися як
  question_set_reference (механізм ВИПАДКОВИХ питань із категорії)
  замість question_reference (пряме посилання на фіксоване питання).
• Підтверджено вихідним кодом Moodle 4.5 (backup_stepslib.php /
  restore_stepslib.php): фіксоване питання в слоті оформлюється як
  <question_instance> з вкладеним <question_reference>, що містить
  <questionbankentryid> — ID запису question_bank_entry у questions.xml.
• mbz_module.py: _xml_quiz() тепер генерує question_reference з
  questionbankentryid для кожного питання; question_set_reference
  прибрано (він створював випадковий набір, який Restore не міг
  зв'язати з конкретними питаннями).
• Додано перевірку узгодженості: questionbankentryid кожного слота
  вказує на реальний question_bank_entry id (100% збіг).
• Тести оновлено: перевірка наявності question_reference, відсутності
  question_set_reference, валідності всіх XML, gzip-сигнатури,
  .ARCHIVE_INDEX.

────────────────────────────────────────────────────────
Серпень 2026 — Виправлення читання .doc (кирилиця) + випадкові питання
────────────────────────────────────────────────────────
• ВИПРАВЛЕНО КРИТИЧНУ ПОМИЛКУ читання .doc на Windows: файли, створені
  у Windows 10 із Word 97-2003 (code page 1251), конвертувалися в
  «?????» — кирилиця повністю втрачалася. Причина: LibreOffice
  --convert-to txt:Text на Windows пише у системну кодову сторінку
  (без кирилиці), бо параметр кодування був відсутній.
  Виправлення converter_core.read_doc_text():
  - LibreOffice тепер викликається з примусовим UTF-8:
    'txt:Text (encoded):UTF8';
  - antiword — із явним мепінгом '-m UTF-8.txt';
  - додано детектор _text_looks_broken(): якщо результат містить
    переважно '?' замість літер — цей метод відкидається і
    спрацьовує наступний fallback (antiword → Word COM → сирий
    UTF-16LE/cp1251). Зіпсований текст більше ніколи не потрапляє
    у результат.
• Випадкові питання у тесті (конструктор курсів .mbz):
  - GlobalSettings.random_questions: 0 = усі питання фіксовані
    (question_reference); N>0 = тест бере N випадкових питань зі своєї
    категорії (question_set_reference + filtercondition);
  - у GUI додано поле «Випадкових питань у тесті (0 = усі за списком)»;
  - grade/sumgrades тесту = N для випадкового режиму;
  - усі питання залишаються в банку (questions.xml) незалежно від режиму.
• Тести: фіксовані (10 question_reference, 0 set_reference) та
  випадкові (5 set_reference, 0 reference, 10 у банку) режими ✔;
  регресія 119 питань, 0 проблем; парсинг наданих .doc (T3=40,
  T1=24 питання, усі з правильною відповіддю) ✔.

────────────────────────────────────────────────────────
Вересень 2026 — Розгалужені сценарії: експорт H5P + JSON
────────────────────────────────────────────────────────
• branching_module.py розширено двома новими експортами:
  - export_h5p(tree): конвертація сценарію у content.json для
    H5P Branching Scenario (H5P.BranchingQuestion 1.0 для вузлів,
    H5P.AdvancedText 1.1 для кінцівок, числові nextContentId,
    startContentId, endScreens, behaviour);
  - export_json(tree): універсальний JSON (формат
    branching-scenario/1.0) із чистою структурою дерева
    (nodes / ends / objectives / guide / gift).
• Двопрохідна індексація зв'язків: спершу всі вузли (індекс 0..N-1),
  потім кінцівки (N..), далі nextContentId кожної альтернативи
  підставляється за реальним індексом цілі (NODE_x/END_x → число).
  Недійсні зв'язки позначаються -1 і виявляються тестом.
• GUI: у вкладці «Розгалужені сценарії» додано кнопки
  «🎓 Експорт H5P (.json)» і «📄 Експорт JSON» зі спільним кроком
  _branch_tree_or_warn() (валідація наявності вузлів).
• Тести експорту: GIFT (181 Б), H5P (4158 Б, 4 елементи, усі
  nextContentId валідні, жодного -1), JSON (2392 Б, 2 вузли,
  2 кінцівки) ✔; регресія 119 питань, 0 проблем ✔.
